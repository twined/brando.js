query Page ($pageId: ID!) {
  page (pageId: $pageId) {
    id
    key
    language
    title
    slug
    data
    status
    css_classes

    parent_id

    meta_description
    meta_keywords

    inserted_at
  }
}
