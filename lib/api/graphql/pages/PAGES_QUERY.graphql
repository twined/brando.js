query Pages {
  pages {
    id
    key
    title
    slug
    language
    parent {
      id
      key
      language
      title
      slug
    }
    children {
      id
      key
      language
      title
      slug
    }
    inserted_at
    updated_at
  }
}
